# Phase 1/2: v1 foundation + compatibility-safe dependency set

# Core runtime
requests>=2.32.3
python-dotenv>=1.0.1
websockets>=13.1
pandas>=2.2.3
numpy>=2.1.3
python-dateutil>=2.9.0

# Web/dashboard stack (Python 3.14 compatible as of current tooling baseline)
fastapi>=0.115.0
uvicorn>=0.30.0
jinja2>=3.1.4

# Structured logging / validation
pydantic>=2.7.0

# Optional for research/export compatibility
pyarrow>=17.0.0

# Keep the default set safe and isolated: no system-wide changes.
# Avoid compiled scientific packages such as TA-Lib until explicitly required.
